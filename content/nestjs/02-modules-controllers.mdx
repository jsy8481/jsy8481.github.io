---
title: "02. 모듈과 컨트롤러"
description: "모듈은 NestJS 애플리케이션의 기본 구성 단위입니다. 관련된 컨트롤러, 서비스, 프로바이더를 하나의 단위로 그룹화합니다."
date: "2026-02-16"
---

## 모듈 (Module) 심화

### 모듈이란?

**모듈** 은 NestJS 애플리케이션의 기본 구성 단위입니다. 관련된 컨트롤러, 서비스, 기타 프로바이더를 하나의 단위로 그룹화합니다.

> 💡 **비유**: 모듈은 레고 블록과 같습니다. 각 블록(모듈)은 독립적으로 작동하며, 다른 블록과 결합하여 더 큰 구조물(애플리케이션)을 만듭니다.

### @Module() 데코레이터

```typescript
import { Module } from '@nestjs/common';
import { StocksController } from './stocks.controller';
import { StocksService } from './stocks.service';
import { DatabaseModule } from '../database/database.module';

@Module({
  imports: [DatabaseModule],       // 1. 의존하는 다른 모듈
  controllers: [StocksController], // 2. HTTP 요청 핸들러
  providers: [StocksService],      // 3. 서비스 및 기타 프로바이더
  exports: [StocksService],        // 4. 외부 공개 대상
})
export class StocksModule {}
```

### 🔍 코드 분석
| 코드 | 설명 |
|------|------|
| `@Module()` | NestJS 모듈 정의 데코레이터입니다. |
| `imports: [DatabaseModule]` | 이 모듈이 **`DatabaseModule`의 exports** 를 필요로 함을 명시합니다. |
| `controllers: [StocksController]` | 이 모듈에 속한 **컨트롤러** 를 등록합니다. NestJS가 인스턴스를 생성하고 라우팅을 연결합니다. |
| `providers: [StocksService]` | 이 모듈에 속한 **서비스(Provider)** 를 등록합니다. DI 컨테이너에 의해 관리됩니다. |
| `exports: [StocksService]` | `StocksService`를 **다른 모듈** 에서도 사용할 수 있도록 공개합니다. (캡슐화 해제) |


**각 속성 설명:**

| 속성 | 역할 | 설명 |
|------|------|------|
| `imports` | 의존성 선언 | 이 모듈에서 사용할 다른 모듈 |
| `controllers` | HTTP 처리 | 라우트 핸들러 클래스 등록 |
| `providers` | 서비스 등록 | DI 시스템에 등록할 클래스 |
| `exports` | 외부 공개 | 다른 모듈에서 사용할 수 있게 공개 |

### 모듈 간 의존성

```typescript
// stocks.module.ts
@Module({
  imports: [
    DatabaseModule,     // DB 연결 필요
    CacheModule,        // 캐싱 필요
    NotificationModule, // 알림 필요
  ],
  // ...
})
export class StocksModule {}
```

** 의존성 규칙:**
- 모듈 A가 모듈 B의 서비스를 사용하려면 → A의 `imports`에 B 추가
- 모듈 B가 서비스를 공개하려면 → B의 `exports`에 해당 서비스 추가

### 모듈 패턴 심화

#### 1. 공유 모듈 (Shared Module)
NestJS의 모듈은 기본적으로 **싱글톤(Singleton)** 입니다.
- `DatabaseModule`을 여러 모듈에서 import해도, 실제 DB 연결 인스턴스는 하나만 생성됩니다.
- 메모리 절약 및 상태 공유에 매우 효율적입니다.

#### 2. 글로벌 모듈 (Global Modules)
헬퍼 함수나 DB 연결처럼 모든 곳에서 필요한 모듈은 `@Global()` 데코레이터를 사용합니다.

```typescript
@Global()
@Module({
  providers: [ValidationPipe],
  exports: [ValidationPipe],
})
export class CoreModule {}
```
> ⚠️ **주의**: 너무 많은 모듈을 글로벌로 만들면 의존성 추적이 어려워집니다. 꼭 필요한 경우(예: DB, Config)에만 사용하세요.

#### 3. 동적 모듈 (Dynamic Modules)
실행 시간에 설정을 받아 모듈을 생성하는 패턴입니다. (예: `TypeOrmModule.forRoot()`)

```typescript
@Module({})
export class ConfigModule {
  static register(options): DynamicModule {
    return {
      module: ConfigModule,
      providers: [{ provide: 'CONFIG', useValue: options }],
      exports: ['CONFIG'],
    };
  }
}
```

> 📘 **Deep Dive**: [Modules](../../../reference/nestjs/OVERVIEW/Modules.md) 문서에서 더 많은 패턴을 확인하세요.

---

## 컨트롤러 (Controller) 심화

### 컨트롤러란?

**컨트롤러** 는 들어오는 HTTP 요청을 처리하고 응답을 반환하는 역할을 합니다. URL 라우팅, 요청 파싱, 응답 포맷팅을 담당합니다.

### @Controller() 데코레이터

```typescript
import { 
  Controller, 
  Get, 
  Post, 
  Put, 
  Delete, 
  Param, 
  Query, 
  Body,
  HttpCode,
  HttpStatus,
} from '@nestjs/common';

@Controller('stocks')  // 기본 경로: /stocks
export class StocksController {
  constructor(private readonly stocksService: StocksService) {}

  // =====================
  // HTTP 메서드 데코레이터
  // =====================

  @Get()  // GET /stocks
  findAll(@Query('exchange') exchange?: string) {
    return this.stocksService.findAll({ exchange });
  }

  @Get(':symbol')  // GET /stocks/AAPL
  findOne(@Param('symbol') symbol: string) {
    return this.stocksService.findBySymbol(symbol);
  }

  @Post()  // POST /stocks
  @HttpCode(HttpStatus.CREATED)  // 응답 코드: 201
  create(@Body() dto: CreateStockDto) {
    return this.stocksService.create(dto);
  }

  @Put(':symbol')  // PUT /stocks/AAPL
  update(
    @Param('symbol') symbol: string,
    @Body() dto: UpdateStockDto,
  ) {
    return this.stocksService.update(symbol, dto);
  }

  @Delete(':symbol')  // DELETE /stocks/AAPL
  @HttpCode(HttpStatus.NO_CONTENT)  // 204 No Content
  remove(@Param('symbol') symbol: string) {
    return this.stocksService.remove(symbol);
  }
}
```

### 🔍 코드 분석
| 코드 | 설명 |
|------|------|
| `@Controller('stocks')` | `/stocks` 경로를 처리하는 컨트롤러입니다. |
| `@Get()` | 기본 GET 요청(`GET /stocks`)을 처리합니다. |
| `@Get(':symbol')` | 경로 변수(`:symbol`)가 포함된 GET 요청(`GET /stocks/AAPL`)을 처리합니다. |
| `@Post()` | POST 요청을 처리합니다. |
| `@HttpCode(HttpStatus.CREATED)` | 성공 시 응답 상태 코드를 **201 Created** 로 설정합니다. (기본 201이지만 명시적으로 표현) |
| `@Put(':symbol')` | PUT 요청(전체 수정)을 처리합니다. |
| `@Delete(':symbol')` | DELETE 요청(삭제)을 처리합니다. |
| `@Param('symbol')` | URL의 `:symbol` 위치에 있는 값을 추출합니다. |
| `@Body()` | 요청 본문을 DTO로 추출합니다. |


### 요청 데이터 추출 데코레이터

| 데코레이터 | 추출 대상 | Express 객체 대응 |
|-----------|----------|-------------------|
| `@Req()`, `@Request()` | 전체 Request 객체 | `req` |
| `@Res()`, `@Response()` | 전체 Response 객체 | `res` (주의 필요) |
| `@Next()` | Next 함수 | `next` |
| `@Session()` | 세션 객체 | `req.session` |
| `@Param(key)` | URL 파라미터 | `req.params[key]` |
| `@Query(key)` | 쿼리 스트링 | `req.query[key]` |
| `@Body(key)` | 요청 본문 | `req.body[key]` |
| `@Headers(name)` | 요청 헤더 | `req.headers[name]` |
| `@Ip()` | 클라이언트 IP | `req.ip` |

> ⚠️ **@Res() 주의사항**:
> `@Res()`를 사용하면 NestJS의 표준 응답 처리(자동 JSON 변환 등)가 비활성화됩니다.
> 특정 기능을 위해 사용하더라도 `passthrough: true` 옵션을 켜는 것이 좋습니다.
> ```typescript
> @Get()
> findAll(@Res({ passthrough: true }) res: Response) {
>   res.cookie('key', 'value'); // 쿠키만 설정하고
>   return []; // 리턴값은 Nest가 처리
> }
> ```

### 예시: 복합 파라미터

```typescript
@Get('search')
// GET /stocks/search?exchange=NASDAQ&sector=Tech&page=1
search(
  @Query('exchange') exchange?: string,
  @Query('sector') sector?: string,
  @Query('page', ParseIntPipe) page: number = 1,
  @Query('limit', ParseIntPipe) limit: number = 20,
) {
  return this.stocksService.search({ 
    exchange, 
    sector, 
    page, 
    limit 
  });
}
```

### 🔍 코드 분석
| 코드 | 설명 |
|------|------|
| `@Query('exchange')` | 쿼리 스트링에서 `exchange` 키의 값을 추출합니다. (`?exchange=NASDAQ`) |
| `ParseIntPipe` | 쿼리 스트링은 기본적으로 문자열이지만, 이를 **숫자(int)** 로 자동 변환하고 유효성을 검사합니다. 숫자가 아니면 400 에러를 냅니다. |
| `limit: number = 20` | 값이 없을 경우 기본값으로 20을 사용합니다. |


**`ParseIntPipe`:**
- 쿼리 스트링은 항상 문자열입니다
- 이 파이프가 자동으로 숫자로 변환합니다
- 변환 실패 시 400 Bad Request 에러 자동 반환

---

## 라우팅 패턴

### 중첩 라우트

```typescript
@Controller('users/:userId/watchlists')
export class WatchlistsController {
  // GET /users/123/watchlists
  @Get()
  findAll(@Param('userId') userId: string) {
    return this.watchlistsService.findByUser(userId);
  }

  // GET /users/123/watchlists/456
  @Get(':id')
  findOne(
    @Param('userId') userId: string,
    @Param('id') id: string,
  ) {
    return this.watchlistsService.findOne(userId, id);
  }
}
```

### 와일드카드 라우트

```typescript
// /stocks/abc 매치
// /stocks/a/b/c 매치
// /stocks/* 패턴
@Get('*')
fallback() {
  throw new NotFoundException('리소스를 찾을 수 없습니다');
}
```

### 버전 관리

```typescript
// v1 API
@Controller({
  path: 'users',
  version: '1',  // /v1/users
})
export class UsersV1Controller {}

// v2 API
@Controller({
  path: 'users',
  version: '2',  // /v2/users
})
export class UsersV2Controller {}
```

---

## 응답 처리

### 기본 응답

```typescript
@Get()
findAll() {
  // 객체/배열 반환 → 자동으로 JSON 변환
  return this.stocksService.findAll();
}
```

### 상태 코드 변경

```typescript
@Post()
@HttpCode(HttpStatus.CREATED)  // 201
create(@Body() dto: CreateStockDto) {
  return this.stocksService.create(dto);
}

@Delete(':id')
@HttpCode(HttpStatus.NO_CONTENT)  // 204 (본문 없음)
remove(@Param('id') id: string) {
  return this.stocksService.remove(id);
}
```

### 커스텀 헤더

```typescript
@Get()
@Header('Cache-Control', 'max-age=3600')
@Header('X-Custom-Header', 'value')
findAll() {
  return this.stocksService.findAll();
}
```

### 리다이렉트

```typescript
@Get('old-path')
@Redirect('https://example.com/new-path', 301)
redirectExample() {
  // 동적 리다이렉트가 필요한 경우
  const url = calculateNewUrl();
  return { url, statusCode: 302 };
}
```

---

## 비동기 처리

### async/await

```typescript
@Get(':id')
async findOne(@Param('id') id: string) {
  // 비동기 작업 대기
  const user = await this.usersService.findOne(id);
  
  if (!user) {
    throw new NotFoundException(`User ${id} not found`);
  }
  
  return user;
}
```

### Observable (RxJS)

```typescript
import { Observable } from 'rxjs';

@Get()
findAll(): Observable<User[]> {
  // RxJS Observable도 지원
  return this.usersService.findAll$();
}
```

---

## 실전 예제: 주식 컨트롤러

```typescript
import { 
  Controller, 
  Get, 
  Post, 
  Param, 
  Query, 
  Body,
  NotFoundException,
  BadRequestException,
} from '@nestjs/common';
import { StocksService } from './stocks.service';
import { CreateStockDto, UpdateStockDto, StockQueryDto } from './dto';

@Controller('stocks')
export class StocksController {
  constructor(private readonly stocksService: StocksService) {}

  @Get()
  async findAll(@Query() query: StockQueryDto) {
    return this.stocksService.findAll(query);
  }

  @Get('trending')
  async getTrending() {
    return this.stocksService.getTrending();
  }

  @Get(':symbol')
  async findBySymbol(@Param('symbol') symbol: string) {
    const stock = await this.stocksService.findBySymbol(symbol.toUpperCase());
    
    if (!stock) {
      throw new NotFoundException(
        `Stock with symbol '${symbol}' not found`
      );
    }
    
    return stock;
  }

  @Get(':symbol/history')
  async getHistory(
    @Param('symbol') symbol: string,
    @Query('period') period: '1D' | '1W' | '1M' | '1Y' = '1M',
  ) {
    return this.stocksService.getHistory(symbol, period);
  }

  @Post()
  async create(@Body() dto: CreateStockDto) {
    // 중복 체크
    const existing = await this.stocksService.findBySymbol(dto.symbol);
    if (existing) {
      throw new BadRequestException(
        `Stock with symbol '${dto.symbol}' already exists`
      );
    }
    
    return this.stocksService.create(dto);
  }
}
```

### 🔍 코드 분석
| 코드 | 설명 |
|------|------|
| `constructor(private stocksService)` | **생성자 주입** 을 통해 비즈니스 로직을 담당하는 서비스를 주입받습니다. |
| `@Get('trending')` | 정적 경로(`trending`)는 동적 경로(`:symbol`)보다 **먼저** 정의해야 합니다. |
| `throw new NotFoundException()` | 리소스가 없을 때 404 에러를 발생시키는 표준 예외입니다. |
| `throw new BadRequestException()` | 잘못된 요청(중복 등)일 때 400 에러를 발생시키는 표준 예외입니다. |


---

## 💡 실무 팁

### 1. 컨트롤러는 얇게 유지

```typescript
// ❌ 나쁜 예: 컨트롤러에 비즈니스 로직
@Get(':id')
async findOne(@Param('id') id: string) {
  const user = await this.db.query.users.findFirst({...});
  const posts = await this.db.query.posts.findMany({...});
  return { ...user, posts };  // 비즈니스 로직이 컨트롤러에!
}

// ✅ 좋은 예: 서비스에 위임
@Get(':id')
async findOne(@Param('id') id: string) {
  return this.usersService.findOneWithPosts(id);  // 서비스에서 처리
}
```

### 2. 라우트 순서 주의

```typescript
// 구체적인 경로가 먼저!
@Get('trending')  // ✅ 먼저 정의
getTrending() {}

@Get(':symbol')   // 나중에 정의
getBySymbol() {}
// 만약 순서가 반대면 'trending'이 symbol로 인식됨
```

### 3. API 접두사 (Global Prefix)
실무에서는 모든 API 경로 앞에 `/api` 또는 `/api/v1`을 붙이는 것이 일반적입니다.

```typescript
// main.ts
app.setGlobalPrefix('api/v1');
// 이제 localhost:3000/api/v1/users 로 접속해야 함
```

---

## 레퍼런스

- [NestJS - Controllers](https://docs.nestjs.com/controllers)
- [NestJS - Modules](https://docs.nestjs.com/modules)
- [Local Reference: Modules](../../../reference/nestjs/OVERVIEW/Modules.md)
- [Local Reference: Controllers](../../../reference/nestjs/OVERVIEW/Controllers.md)
- [NestJS - Route Parameters](https://docs.nestjs.com/controllers#route-parameters)

**다음 장**: [03. 서비스와 프로바이더](./03-services-providers.md)
